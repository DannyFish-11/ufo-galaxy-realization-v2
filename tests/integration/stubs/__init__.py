"""tests/integration/stubs — minimal contract stubs for NL canonical path CI proof.

These stubs simulate real infrastructure responses (LLM backends, etc.) without
requiring live API keys.  They are used exclusively in integration tests to
exercise the actual canonical path code while remaining CI-safe.
"""
