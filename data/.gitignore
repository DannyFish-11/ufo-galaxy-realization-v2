# Runtime data — not committed
rag_memory/
*.jsonl
*.db
*.sqlite
*.json

# 从 GitHub 接进来的项目落在这儿。**整个目录都不进仓库** ——
# 它是运行时产物(第三方仓库的完整副本),而且测试如果没隔离干净会往这里写。
# 这一条是踩出来的:一条端到端用例真的往这儿克隆了一份 README 并被提交了上去。
github_addons/
