{
  "_comment": "API 面对齐台账。记录『galaxy_gateway.app 有、而权威层(core.api_routes + core.health_check)没有』的路径,以及每一条**为什么还没并进去**。空的才是理想状态;这里非空是因为剩下的都是重复族,并进去只会让一个 app 里出现两套做法。tests/test_api_surface_parity.py 会盯住它不再变长。",
  "_measured_at_commit": "见 git 历史;用 tests/test_api_surface_parity.py 重新测量",
  "authoritative_count": 373,
  "gateway_count": 59,
  "gateway_only": [
    {
      "path": "/api/commands",
      "reason": "网关的无版本前缀族;权威层用 /api/v1/*。并存会造成同一能力两个 URL。"
    },
    {
      "path": "/api/devices",
      "reason": "同上 —— A 已有 /api/devices/list|register|heartbeat 与 /api/v1/devices/*。"
    },
    {
      "path": "/api/devices/connected",
      "reason": "同上 —— A 已有 /api/devices/list|register|heartbeat 与 /api/v1/devices/*。"
    },
    {
      "path": "/api/devices/{device_id}",
      "reason": "同上 —— A 已有 /api/devices/list|register|heartbeat 与 /api/v1/devices/*。"
    },
    {
      "path": "/api/tasks",
      "reason": "同上 —— A 用 /api/v1/tasks/*。"
    },
    {
      "path": "/api/tasks/{task_id}",
      "reason": "同上 —— A 用 /api/v1/tasks/*。"
    },
    {
      "path": "/api/v1/config",
      "reason": "与 core/routes/config.py 的 /api/config 同为『取配置』,响应形状不同。合并前要先定哪个是契约。"
    },
    {
      "path": "/api/v1/gateway/metrics",
      "reason": "与 /api/v1/health 同一个 router(galaxy_gateway/routes/health.py),整体依赖 app.state。"
    },
    {
      "path": "/api/v1/gateway/metrics/json",
      "reason": "与 /api/v1/health 同属 galaxy_gateway/routes/health.py,该 router 整体依赖 app.state(_get_state(request, ...)),权威层不跑 galaxy_gateway 的 lifespan,并过来会恒 503。网关自身的 Prometheus 指标本来也更适合留在网关进程里。"
    },
    {
      "path": "/api/v1/health",
      "reason": "同上(_get_state(request,...) 取 app.state)。实测并过来恒 503 {\"detail\":\"Service not ready\"}。权威层已有 /api/v1/health/quick 与 /unified,功能不缺。"
    },
    {
      "path": "/api/v1/llm/stats",
      "reason": "依赖 galaxy_gateway 的 app.state(Depends(get_llm_router)),权威层不跑它的 lifespan。实测并过来会恒 503 —— 搬迁前要先让处理函数改从 core 单例取。"
    },
    {
      "path": "/api/v1/pairing/approve",
      "reason": "设备配对有两套独立实现,词汇不同:core/routes/pairing.py 用 /api/v1/pair/*(card/claim/peers/trust/check),galaxy_gateway/api/pairing.py 用 /api/v1/pairing/*(code/enroll/approve/deny/pending/status)。把两套一起挂上去 = 一个 app 里两个配对系统,已配对的设备在另一套里不可见。保留哪一套、旧数据怎么迁,是产品决定,不该由接线顺手定。"
    },
    {
      "path": "/api/v1/pairing/claim/{request_id}",
      "reason": "设备配对有两套独立实现,词汇不同:core/routes/pairing.py 用 /api/v1/pair/*(card/claim/peers/trust/check),galaxy_gateway/api/pairing.py 用 /api/v1/pairing/*(code/enroll/approve/deny/pending/status)。把两套一起挂上去 = 一个 app 里两个配对系统,已配对的设备在另一套里不可见。保留哪一套、旧数据怎么迁,是产品决定,不该由接线顺手定。"
    },
    {
      "path": "/api/v1/pairing/code",
      "reason": "设备配对有两套独立实现,词汇不同:core/routes/pairing.py 用 /api/v1/pair/*(card/claim/peers/trust/check),galaxy_gateway/api/pairing.py 用 /api/v1/pairing/*(code/enroll/approve/deny/pending/status)。把两套一起挂上去 = 一个 app 里两个配对系统,已配对的设备在另一套里不可见。保留哪一套、旧数据怎么迁,是产品决定,不该由接线顺手定。"
    },
    {
      "path": "/api/v1/pairing/deny",
      "reason": "设备配对有两套独立实现,词汇不同:core/routes/pairing.py 用 /api/v1/pair/*(card/claim/peers/trust/check),galaxy_gateway/api/pairing.py 用 /api/v1/pairing/*(code/enroll/approve/deny/pending/status)。把两套一起挂上去 = 一个 app 里两个配对系统,已配对的设备在另一套里不可见。保留哪一套、旧数据怎么迁,是产品决定,不该由接线顺手定。"
    },
    {
      "path": "/api/v1/pairing/enroll",
      "reason": "设备配对有两套独立实现,词汇不同:core/routes/pairing.py 用 /api/v1/pair/*(card/claim/peers/trust/check),galaxy_gateway/api/pairing.py 用 /api/v1/pairing/*(code/enroll/approve/deny/pending/status)。把两套一起挂上去 = 一个 app 里两个配对系统,已配对的设备在另一套里不可见。保留哪一套、旧数据怎么迁,是产品决定,不该由接线顺手定。"
    },
    {
      "path": "/api/v1/pairing/pending",
      "reason": "设备配对有两套独立实现,词汇不同:core/routes/pairing.py 用 /api/v1/pair/*(card/claim/peers/trust/check),galaxy_gateway/api/pairing.py 用 /api/v1/pairing/*(code/enroll/approve/deny/pending/status)。把两套一起挂上去 = 一个 app 里两个配对系统,已配对的设备在另一套里不可见。保留哪一套、旧数据怎么迁,是产品决定,不该由接线顺手定。"
    },
    {
      "path": "/api/v1/pairing/status/{request_id}",
      "reason": "设备配对有两套独立实现,词汇不同:core/routes/pairing.py 用 /api/v1/pair/*(card/claim/peers/trust/check),galaxy_gateway/api/pairing.py 用 /api/v1/pairing/*(code/enroll/approve/deny/pending/status)。把两套一起挂上去 = 一个 app 里两个配对系统,已配对的设备在另一套里不可见。保留哪一套、旧数据怎么迁,是产品决定,不该由接线顺手定。"
    },
    {
      "path": "/api/v1/sessions/stats",
      "reason": "网关自带的 sessions 路由与 core/routes/sessions.py 是两套;A 已有 /api/v1/sessions/*。"
    },
    {
      "path": "/api/v1/sessions/{session_id}/migrate",
      "reason": "同上,且与 A 的 /api/v1/sessions/migrate 同能力异形(路径参数 vs 请求体)。"
    },
    {
      "path": "/api/v1/webrtc/endpoint",
      "reason": "网关侧 WebRTC 端点;权威层没有对应族,但它依赖网关内部状态,搬迁需先解耦。"
    },
    {
      "path": "/gateway/metrics",
      "reason": "同 /api/v1/gateway/metrics/json,只是没有版本前缀 —— 这条本身也是历史遗留的双写法,真要保留应当只留带版本的那条。属于 health.py 那一整块,一起解耦时再定。"
    }
  ]
}
