# Galaxy - Windows-Only Dependencies (仅Windows安装)
# =====================================================
# 安装命令: pip install -r requirements-windows.txt
# 这些依赖仅在Windows平台有效，其他平台会自动跳过

# ============================================================================
# Desktop UI — PyQt5界面 (如需桌面GUI)
# ============================================================================
PyQt5>=5.15.0; sys_platform == 'win32'
pyqtwebengine>=5.15.0; sys_platform == 'win32'

# ============================================================================
# Windows System API — 系统控制
# ============================================================================
pywin32>=306; sys_platform == 'win32'
comtypes>=1.2.0; sys_platform == 'win32'

# ============================================================================
# Desktop Automation — UI操作
# ============================================================================
pyautogui>=0.9.54
pytesseract>=0.3.10
pygetwindow>=0.0.9; sys_platform == 'win32'
pywinauto>=0.6.8; sys_platform == 'win32'
pynput>=1.7.0
keyboard>=0.13.5

# ============================================================================
# Speech — Windows语音合成/识别
# ============================================================================
pyttsx3>=2.90; sys_platform == 'win32'
SpeechRecognition>=3.10.0

# ============================================================================
# Sound
# ============================================================================
sounddevice>=0.4.0
