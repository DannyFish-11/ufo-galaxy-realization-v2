# Galaxy - Development Dependencies
# =====================================

# ============================================================================
# Code Formatting
# ============================================================================
# 真 bug 修复(CI 排查):此前是浮动下限(>=)，没有上限——black/isort 各自
# "稳定风格"会随新版本悄悄变化(black 的 stable style 明确保留跨版本调整的
# 权利)，CI 每次都装最新版，会让【完全没改过的文件】突然被判定"格式不对"，
# 之前已合并、CI 曾经全绿的提交(如本仓库 98cd456/74f5aef 触及的文件)也会
# 无端遭殃——这正是 lint 门反复无常报红的根因，不是代码真的写错了。锁定
# exact 版本(=当前已验证与仓库现状一致的版本)，消除这整类漂移。
black==26.5.1
isort==8.0.1

# ============================================================================
# Code Analysis
# ============================================================================
flake8==7.3.0
pylint>=3.0.3
mypy>=1.8.0

# ============================================================================
# Testing
# ============================================================================
pytest>=7.4.4
pytest-asyncio>=0.23.3
pytest-cov>=4.1.0
pytest-mock>=3.12.0
# 真 bug 修复(CI 排查):没有它,**挂死的测试永远不会中止**。
#
# 实测:`test` 作业连续多次在跑到 99% 时静默卡住 —— 单条测试进去就再没出来,直到
# runner 整体被回收(GitHub 打出 "The runner has received a shutdown signal"),于是
# 整轮 4 万多条测试**一个结论都拿不到**,既看不到失败清单,也看不到卡在哪。main 自己
# 也有同样签名(某次 test 作业卡 64 分钟被杀,同 run 其它 16 个作业全绿)。
#
# 装上之后,超时的那条会被打断并如实报成 failure,**整轮继续跑完** —— 这才拿得到完整
# 的失败清单,traceback 也直接指出卡在哪一行。已实测两种真实挂法(等永不释放的锁、
# 长 sleep)都能被打断且不影响后续用例。
pytest-timeout>=2.3.0
coverage>=7.4.0

# ── 可选运行时依赖:对**测试**必须在场 ──────────────────────────────────
#
# webrtcvad 对运行时是可选的(它在 requirements.txt 的可选块里注释着,语音唤醒
# 才用得上),但它对 core/multimodal/vad.py 是**主判据**:可用时由它先判"像不像
# 人声",不可用才回退纯能量法。
#
# 不装它的后果是真栽过的:**主判据那条路径从来没在 CI 上跑过**。于是
#   · webrtcvad 独断、噪声底逻辑被完全旁路 —— 风扇档白噪声判活 100%
#   · 一批为纯能量 VAD 写的用例悄悄开始依赖"webrtcvad 不存在"
# 两件事在 CI 上都看不见,而在任何装了它的机器(开发机、真机)上全线复现 ——
# 真机症状就是「一启用就一直判定有人在说话」。
#
# 装进 dev 依赖 = 测试环境有它、生产仍可选。两条路径这才都被覆盖:
# 主判据由真 webrtcvad 跑,回退路径由显式 use_webrtc=False 的用例跑。
webrtcvad-wheels>=2.0.14 ; platform_system != "Emscripten"

# aiortc —— 设备实时语音通话的媒体面(core/voice_call_bridge)。
#
# 与 webrtcvad-wheels 同一个理由进 dev 依赖:这条链路上会出错的地方几乎全在"真的传
# 一遍"才暴露 —— 重采样把音调弄错、出站轨不按时间产帧导致对端听到加速播放、帧的
# pts/time_base 不对导致轨被判无效、打断时缓冲没清干净所以 AI 又硬说完半句。把
# RTCPeerConnection mock 掉,以上每一条都会被"测试通过"盖住。
#
# 装进 dev = 测试环境有它、生产仍可选(webrtc_available() 缺了会如实说明原因并拒绝
# 通话,不静默假装接通)。test_voice_call_bridge_real_webrtc 里另有一条守卫,确保这个
# 依赖被拿掉时测试是**红**的而不是静默跳过。
aiortc>=1.9.0 ; platform_system != "Emscripten"
