# Core Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# WebSocket support (required for gateway WS endpoints and gateway tests)
websockets>=11.0

# HTTP Client
httpx>=0.25.0

# AI/LLM Integration (if needed by nodes)
# Aligned with root requirements.txt: openai>=1.0.0 (was >=2.0.0, which conflicted)
openai>=1.0.0
