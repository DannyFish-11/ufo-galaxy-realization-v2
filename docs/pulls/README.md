# docs/pulls/

Archived GitHub pull-request metadata snapshots.

These JSON files are informational records of PR state at various points.
They are not used by any automated tooling.

Relocated from the repository root `pulls/` directory as part of root hygiene
(PR: harden-tests-deployment-ci-rules).
