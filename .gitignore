# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
# Exception: Electron 控制面板的构建产物需要随仓库分发，供 Electron 直接加载。
!electron/renderer/panel/dist/
!electron/renderer/panel/dist/**
downloads/
eggs/
.eggs/
lib/
# 例外:面板前端源码目录 src/lib 被上面的 Python 构建残留规则 lib/ 误伤——
# api.ts(SSE 客户端)因此从未进过仓库,克隆方只能用预编译 dist、无法从源码
# 构建面板。显式放行。
!electron/renderer/panel/src/lib/
!electron/renderer/panel/src/lib/**
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment
.env
.env.local
*.local

# Per-machine persisted choices (main-brain model / container runtime) — 本机状态，非源码
.galaxy_model
.galaxy_runtime
.galaxy_revoked_tokens.json
# Mesh 主密钥 —— capability_token 的签名密钥,同时也是 agent_card 的名片签名密钥。
# 此前只忽略了撤销表却漏了密钥本身:该文件由 core/capability_token.py 首次签发时
# 自动生成(0600),泄露等于任何人都能伪造能力令牌与配对名片。绝不入库。
.galaxy_mesh_key
# 对端信任档案(peer_trust)与配对产生的本机运行时数据
data/peer_trust.json

# Logs
*.log
logs/

# Database (global)
*.sqlite3

# Runtime artifacts — forbidden inside node directories (and anywhere else)
*.pid

# Node-directory runtime artifacts: databases, logs, caches
nodes/**/*.db
nodes/**/*.sqlite
nodes/**/*.sqlite3
nodes/**/*.log
nodes/**/*.pid
nodes/**/__pycache__/
nodes/**/.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Android
android_client/.gradle/
android_client/build/
android_client/app/build/
android_client/local.properties
android_client/.idea/
android_client/*.iml
android_client/captures/
android_client/.externalNativeBuild/
android_client/.cxx/

# Windows
windows_client/__pycache__/
*.exe
*.dll

# Test
.pytest_cache/
.coverage
htmlcov/

# Temporary
*.tmp
*.temp
.cache/

# Auto-generated nodes created by AutonomousCoder._deploy_as_node
nodes/Node_*_AutoGenerated/
dashboard/frontend/node_modules/
dashboard/frontend/dist/

# Knowledge Core runtime data directories
unified_kb/
knowledge_db/
data/rag_memory/
# 统一记忆层运行时数据（不入库）
data/omni_simplemem/
data/_verify_chroma/
data/_verify_clip/
data/_verify_clip2/
data/_verify_clip3/
data/_verify_clip_mock/
data/clip_memory/
data/clap_memory/
chroma_db/
chroma/

# Local unified configuration authority (PR-3)
# Real operator config and secrets must not be committed.
# Commit only the *.example.* templates in runtime/.
runtime/config.json
runtime/secrets.env
runtime/task_allocation_truth.json
# 主脑选择等本机持久化状态（unified_launcher 启动时写)。漏了这条的后果是:
# 任何人跑一次 main.py,工作区就多一个未跟踪文件 —— 与上面那句"runtime/ 里
# 只提交 *.example.* 模板"的既定意图矛盾。实跑一次启动才发现的。
runtime/model_state.json
data/runtime/delegated_runtime_execution_tracker.json
learning_data.db
audit/wiring_report_*.json
electron/node_modules/
electron/renderer/panel/node_modules/

# npm lockfiles —— 现在必须入库（B10）
#
# 此前这里是 `package-lock.json` + `electron/package-lock.json` 两条忽略规则，
# 注释写着 "npm lockfiles are not tracked in this repo"。后果是三处
# package.json 全无 lock：每次 `npm install` 都可能拉到不同的传递依赖树，
# 构建不可复现，供应链上任何一个上游发新版都会静默改变产物。
#
# 这同时卡死了 panel 的 dist 一致性门（B22/P14）：没有 lock，重建产物的内容
# 哈希会随依赖漂移而变，那个门必然假红，于是根本立不起来。
#
# 现已生成并提交 lock，CI 用 `npm ci`（严格按 lock 安装）而非 `npm install`。
# 仍然忽略 node_modules（见上方两条）。

# SQLite sidecar files (WAL mode)
*.db-shm
*.db-wal

# Panel runtime state written by unified_panel_aggregation during tests
runtime/panel_last_operator_action.json

# Client-discovery entrypoint written by unified_launcher on every launch
runtime/entrypoint.json

# 节点一键启停的本机 PID 状态(core/node_lifecycle.py 运行时生成,勿入库)
runtime/node_pids.json

# OAuth 连接器 token/凭据本机存储(勿入库)
runtime/oauth_connectors.json

# 插件注册表是【运行时产物】：Node_115_PluginManager 启动时会把内置插件写进来，
# 跑测试同样会生成。它不是源码，误提交会让每台机器的本地状态互相覆盖。
plugins/registry.json
