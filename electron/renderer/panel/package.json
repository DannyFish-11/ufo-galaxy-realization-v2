{
  "name": "galaxy-hud",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "UFO Galaxy 面板。纯 TypeScript,不依赖任何 UI 框架,也不依赖任何桌面外壳。",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
